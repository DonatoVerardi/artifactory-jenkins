version: '3.7'
services:
  jenkins:
    image: jenkins/jenkins:lts   ## 2.235.2    
    privileged: true
    user: root
    ports:
      - 8081:8080
      - 50000:50000
    container_name: jenkins
    volumes:
      - .\\jenkins:/var/jenkins_home